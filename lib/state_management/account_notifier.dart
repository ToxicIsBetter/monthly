// lib/state_management/account_notifier.dart
import 'package:flutter/material.dart';
// import 'package:monthly/models/account.dart'; // Will use later

class AccountNotifier extends ChangeNotifier {
  // List<Account> _accounts = []; // Will store accounts here
  // double _totalBalance = 0.0;

  // Add methods related to accounts (e.g., addAccount, updateBalance)
}
